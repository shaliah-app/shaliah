/**
 * A utility function to enable syntax highlighting for CSS in template literals.
 * This function uses String.raw to allow VSCode to recognize the content as CSS
 * for better IntelliSense and code highlighting.
 */
export const css = String.raw;
